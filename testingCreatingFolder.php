<?php


    /* To create a folder we use
     * mkdir("./resources/users/shalom_wilson70",0777);
     *
     * 1 - read
     * 2 - write
     * 4 - Execute
     * To change the permission on a folder we use
     * chmod("file path",permission_code)
     *
     * To remove a folder recursively we will use the shell_exec() command
     *
     * For Example:-
     *
     * shell_exec("rm -R ./resources/users/abc");
     *
     * This will remove the folder name "abc" and all its child folder
     *
     *
     */

?>

